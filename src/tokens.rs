#[derive(Debug, PartialEq, Clone)]
pub enum Tokens {
    LET,
    EQUALS,
    IDENT(String),
    NUMBER(i32),
    STRING(String),
    SEMICOLON,
    PLUS,
    MINUS,
    DOUBLECOL,
    OPENCURLY,
    CLOSECURLY,
    OPENPAREN,
    CLOSEPAREN,
    VOID,
    INT,
    RETURN,
    EOF,
}
